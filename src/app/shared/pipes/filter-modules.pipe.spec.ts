import { FilterModulesPipe } from './filter-modules.pipe';

describe('FilterModulesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModulesPipe();
    expect(pipe).toBeTruthy();
  });
});
